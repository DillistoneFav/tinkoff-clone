import React, {FC} from 'react';
import Layout from "../Layout/Layout";
import {StyledText} from "../UI/styles";

const Auth: FC = () => {
    return (
        <Layout>
            <StyledText>auth</StyledText>
        </Layout>
    );
};

export default Auth;
